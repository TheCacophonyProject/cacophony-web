//thermal:
  //* file sizes/lengths
  //* recordingDateTime vs createdAt, etc
  //* locations
  //* comments
  //
  //audio:
  //* file sizes / lengths
  //* recordingDateTime vs createdAt, etc
  //* locations
  //* comments
  //* battery, airplane
  //
  //
  //Processing tests:
  //* check processing, processingStartTime updated on pick up
  //* check file in analyse picked up
  //* check audio files (as above)
  //* check oldest recording picked up
  //* check file for device with alerts prioritised (oldest first)
  //* check files with processing=true not picked up
  //* check files with a processingStrtTime picked up if processing=false
  //* check files in 'analyse.failed', 'reprocess.failed' 'FINISHED' and 'CORRUPT' ignored
  // 
  //Tags:
  // Test no tags
  // Test one tag
  // Test many tags
  // Test all tag parameters set/read
  //
  // TODO: test in tags - will need to test boith Tag and upload endpoint
  //
  //
  // Tracks:
  // Test no tracks
  // Test one track
  // Test many tracks
  // Test all track parameters set/read
  //
  // TrackTags:
  // Test no tracktags
  // Test one tracktag
  // Test many tracktags
  // Test all tracktag parameters set/read
  //
  // Thumbnails:
  // *Auth
  // *Get thumbnail - need way to insert one
  // *Get thumbnail for recording with no thumbnial
  // *Invalid recording ids
  //
  //* Mark for reprocessing:
  //* Auth
  //* Check marked
  //* Check picked up
  //* Check invalid values handled
  //
  // *Query recording / query recording count
  //
  // Visits: 
  // TODO: DEPRACATED
  //
  // Update recording:
  // *Auth
  // *location
  // *comment
  // *additionalMetadata
  // *invalid recording
  // *invalid values
  //
  // Needs-tag:
  // Auth
  // deviceId - bias???
  // compare values against other endpoiunts

