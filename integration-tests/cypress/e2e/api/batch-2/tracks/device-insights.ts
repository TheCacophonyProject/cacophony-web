describe("Tracks: insights", () => {
  it("Can get track positional data for a given tag in order to produce a heatmap", async () => {
    // TODO:
    // Add recordings, add tags to recording, pull in data and check that positional data is present
    return;
  });
});
