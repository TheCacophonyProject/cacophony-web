import {test} from "@playwright/test";

test("When setting up a new device and setting a new location via sidekick, device history location should be updated.", async () => {
    //
});

test("Device config events carrying location information should carry forward device history settings info", async () => {

});